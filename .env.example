# Free Gemini API key for the fast $0 stack (docker-compose.free.yml).
# Get one at https://aistudio.google.com/apikey  — then copy this file to `.env`.
GEMINI_API_KEY=

# Optional overrides (defaults target Gemini):
# VLLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# VLLM_MODEL_NAME=gemini-2.0-flash
