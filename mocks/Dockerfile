FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY mock_tei.py mock_vllm.py ./
# The command (which mock + port) is provided by docker-compose.
